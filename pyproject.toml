[project]
name = "distributed-audio-transcription"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.5.0",
    "fabric>=3.2.3",
    "faster-whisper>=1.2.1",
    "omegaconf>=2.3.1",
    "tqdm>=4.70.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/distributed_audio_transcription"]
